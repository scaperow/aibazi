name: CI/CD

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - name: Checkout code
        uses: actions/checkout@v2
 
      # Set up Node.js
      - name: Set up Node.js 
        uses: actions/setup-node@v2
        with:
          node-version: '16' # 根据你的项目需要修改 Node.js 版本

      # Install dependencies
      - name: Install dependencies
        run: npm install

      # Build the project
      - name: Build the project
        run: npm run build # 确保你的 package.json 中定义了 build 脚本

      # Install OSS CLI
      - name: Install Aliyun OSS CLI
        run: |
          npm install -g aliyun-cli
      # Configure OSS credentials
      - name: Configure OSS credentials
        run: |
          aliyun configure set --access-key-id ${{ secrets.ALIYUN_ACCESS_KEY_ID }} --access-key-secret ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }} --region cn-beijing
      # Upload to OSS
      - name: Upload to Aliyun OSS
        run: |
          aliyun oss cp dist/  oss://aibazi/ -e oss-cn-beijing.aliyuncs.com -r -f

      - name: Sleep for 30 seconds
        run: sleep 30s
        shell: bash
     
      - name: Refresh CDN
        run: |
          aliyun cdn RefreshObjectCaches --ObjectPath https://aibazi.scaperow.com/index.html
      
      # - name: Notify via Server酱 (Success)
      #   if: success()  # 只有成功时才发送通知
      #   run: |
      #     curl -X POST "https://sctapi.ftqq.com/${{ secrets.SERVERCHAN_SEND_KEY }}.send" \
      #     -d "title=Aliyun OSS Deployment Success" \
      #     -d "desp=The project(aibazi) has been successfully deployed to Aliyun OSS."

      # # Notify deployment failure via Server酱
      # - name: Notify via Server酱 (Failure)
      #   if: failure()  # 部署失败时发送通知
      #   run: |
      #     curl -X POST "https://sctapi.ftqq.com/${{ secrets.SERVERCHAN_SEND_KEY }}.send" \
      #     -d "title=Aliyun OSS Deployment Failure" \
      #     -d "desp=The deployment to Aliyun OSS has failed. Please check the workflow logs for details."